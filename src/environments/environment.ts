const basePath = "http://www.localhost:3000/api/";
const siteUrl = "http://localhost/v3/";

export const environment = {
  production: false,
  apiPaths: {
    product: basePath+"product/",
    images: basePath+"images/",
    category: basePath+"category/"
  }
};